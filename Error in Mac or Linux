Those who are not using Windows will get fatal error in "#include<conio.h>" as conio is not recognised as a standard c library.
The function used from this header file are "getch()", replace it with "getchar()".
If "system("cls")" produce any error try replacing it with "system("clear")".
